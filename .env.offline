# Loaded by `npm run serve:offline` (vue-cli-service --mode offline).
# Routes all API calls to the in-memory mock backend (src/mockApi.js)
# so the client runs without the server.
NODE_ENV=development
VUE_APP_OFFLINE=true
